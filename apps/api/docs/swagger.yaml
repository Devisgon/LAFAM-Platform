openapi: 3.0.3
info:
  title: LAFAM API
  version: 0.1.0
  description: LAFAM Admin Dashboard API contract.
servers:
  - url: http://localhost:4000
    description: Local development server
paths:
  /health:
    get:
      summary: Health check
      tags:
        - Core
      responses:
        "200":
          description: API is running
